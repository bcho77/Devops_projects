apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-app-ch.fullname" . }}
  labels:
    {{- include "web-app-ch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-app-ch.selectorLabels" . | nindent 4 }}
