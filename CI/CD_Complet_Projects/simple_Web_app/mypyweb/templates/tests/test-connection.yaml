apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mypyweb.fullname" . }}-test-connection"
  labels:
    {{- include "mypyweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mypyweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
